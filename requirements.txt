# Runtime dependencies for MatchFlow.
# Shared scientific/Spark deps are PINNED to exact versions so that installing
# sparkly, delex, and MatchFlow into the same environment always resolves to one
# mutually-tested set (no version drift between the packages). These pins match
# the repo-root constraints.txt. Package-local web/util leaf deps (flask,
# requests, streamlit, tabulate) keep a floor (>=) since they do not participate
# in the cross-package shared set.
flask>=2.0.0
joblib==1.2.0
mmh3==3.0.0
numba==0.60.0
numpy==1.26.0
pandas==2.2.3
py_stringmatching==0.4.7
pyarrow==14.0.0
pyspark==3.5.3
requests>=2.25.0
scikit_learn==1.3.1
scipy==1.15.2
streamlit>=1.0.0
tabulate>=0.8.9
threadpoolctl==3.1.0
tqdm==4.64.1
xxhash==3.2.0
